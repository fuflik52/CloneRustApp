# Настройка карты сервера

## Описание
Скрытая страница `/map` отображает карту сервера в реальном времени с позициями всех игроков.

## Установка

### 1. Установите плагин PanRust на сервер Rust

Плагин **PanRust.cs** уже включает функционал синхронизации карты. Просто установите его:

1. Скопируйте `PanRust.cs` в папку `oxide/plugins/` на вашем сервере
2. Установите плагин **MapImageUrl.cs** (генерирует изображение карты)
3. Подключите плагин к панели командой в консоли сервера:

```bash
panrust.pair YOUR_SECRET_KEY
```

Секретный ключ можно найти в веб-панели в разделе "Серверы".

### 2. Проверьте работу

После перезагрузки плагина в консоли должно появиться:

```
[PanRust] PanRust: Подключен
[PanRust] Map URL: https://files.facepunch.com/rust/map-images/...
[PanRust] Map URL sent successfully
```

## Использование

### Доступ к карте

Карта доступна по адресу:
```
http://your-domain.com/map
```

Например:
```
http://localhost:3000/map
```

Можно указать конкретный сервер:
```
http://localhost:3000/map?server=SERVER_ID
```

### Особенности

- ✅ Карта обновляется автоматически каждые 5 секунд
- ✅ Отображаются все онлайн игроки с их именами
- ✅ Цветовая индикация: зеленый = в команде, красный = соло
- ✅ При наведении на игрока показывается его аватар и Steam ID
- ✅ Карта скрыта от обычных пользователей (не отображается в меню)
- ✅ Автоматическое подключение через существующий API ключ

### Легенда

- 🟢 **Зеленая точка** - игрок в команде
- 🔴 **Красная точка** - игрок без команды (соло)

## Как это работает

1. Плагин **MapImageUrl** генерирует изображение карты с сеткой
2. Плагин **PanRust** отправляет URL карты и позиции игроков на веб-сервер
3. Веб-страница `/map` получает данные через API и отрисовывает игроков на карте
4. Позиции обновляются каждые 5 секунд вместе с обычным состоянием сервера

## API Endpoints

### GET /api/servers/:serverId/map
Получить данные карты и позиции игроков

**Ответ:**
```json
{
  "mapUrl": "https://...",
  "worldSize": 4000,
  "players": [
    {
      "steam_id": "76561198...",
      "name": "Player Name",
      "avatar": "https://...",
      "position": { "x": 100, "y": 50, "z": -200 },
      "team": "12345"
    }
  ],
  "serverName": "My Server",
  "online": 10
}
```

### POST /api/map-url
Обновить URL карты (используется плагином)

**Тело запроса:**
```json
{
  "mapUrl": "https://...",
  "worldSize": 4000,
  "hostname": "server.ip.address"
}
```

## Troubleshooting

### Карта не загружается
- Проверьте, что плагин MapImageUrl установлен и работает
- Выполните команду `mapurl` в консоли сервера
- Убедитесь, что URL карты доступен

### Игроки не отображаются
- Проверьте, что плагин PanRust подключен: `panrust.status`
- Проверьте логи сервера на наличие ошибок
- Убедитесь, что веб-сервер запущен

### Позиции не обновляются
- Плагин обновляет позиции каждые 5 секунд вместе с состоянием сервера
- Проверьте, что сервер может подключиться к API
- Проверьте логи: должно быть `[PanRust] Map URL sent successfully`

## Безопасность

⚠️ **Важно:** Страница `/map` не защищена паролем. Если вы хотите скрыть её от посторонних:

1. Добавьте авторизацию на уровне веб-сервера (nginx/apache)
2. Или измените код страницы, добавив проверку пароля
3. Или используйте VPN для доступа к панели

## Преимущества интеграции в PanRust

- ✅ Один плагин вместо двух
- ✅ Автоматическое подключение через существующий API ключ
- ✅ Не нужно настраивать отдельно
- ✅ Позиции игроков отправляются вместе с обычными данными
- ✅ Меньше нагрузка на сервер
